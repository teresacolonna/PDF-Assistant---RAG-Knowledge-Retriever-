version: '3.8'

services:
  rag:
    build: .
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env:ro
    ports:
      - "8000:8000"    
    command: ["uvicorn", "src.app:app", "--host", "0.0.0.0", "--port", "8000"]
